{
    "username": "${CLOUD_USER}",
    "status": 1,
    "password": "${CLOUD_PASS}",
    "home_dir": "/srv/sftpgo/data/${CLOUD_USER}",
    "permissions": {
      "/": ["*"]
    },
    "quota_size": 0,
    "quota_files": 0,
    "filesystem": {
      "provider": 0,
      "params": {
        "base_dir": "/srv/sftpgo/data/${CLOUD_USER}"
      }
    }
  }