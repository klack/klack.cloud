apiVersion: 1

providers:
  - name: 'My Dashboard Provider'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards  # Path to the folder with the dashboards
